# Claude project instructions

Follow the existing project conventions. Keep changes small, tested, and easy to review. Prefer minimal fixes over broad rewrites.

Before proposing or committing code, inspect the existing implementation and tests. Run the repo's normal validation commands when available.

Do not merge PRs. Do not expose secrets. Do not make unrelated formatting-only changes.

For review behavior, see `REVIEW.md`.
